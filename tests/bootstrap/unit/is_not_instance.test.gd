extends WAT.Test

func title():
	return "Given an Is Not Instance of class/type Assertion"

func test_when_calling_is_not_AABB():
	describe("when calling asserts.is_not_AABB(null)")

	asserts.is_not_AABB(null, "Then it passes")

func test_when_calling_is_not_Array():
	describe("when calling asserts.is_not_Array(null)")

	asserts.is_not_Array(null, "Then it passes")

func test_when_calling_is_not_Basis():
	describe("when calling asserts.is_not_Basis(null)")

	asserts.is_not_Basis(null, "Then it passes")

func test_when_calling_is_not_bool():
	describe("when calling asserts.is_not_bool(null)")

	asserts.is_not_bool(null, "Then it passes")

func test_when_calling_is_not_Color():
	describe("when calling asserts.is_not_Color(null)")

	asserts.is_not_Color(null, "Then it passes")

func test_when_calling_is_not_Dictionary():
	describe("when calling asserts.is_not_Dictionary(null)")

	asserts.is_not_Dictionary(null, "Then it passes")

func test_when_calling_is_not_float():
	describe("when calling asserts.is_not_float(null)")

	asserts.is_not_float(null, "Then it passes")

func test_when_calling_is_not_int():
	describe("when calling asserts.is_not_int(null)")

	asserts.is_not_int(null, "Then it passes")

func test_when_calling_is_not_NodePath():
	describe("when calling asserts.is_not_NodePath(null)")

	asserts.is_not_NodePath(null, "Then it passes")

func test_when_calling_is_not_Object():
	describe("when calling asserts.is_not_Object(null)")

	asserts.is_not_Object(null, "Then it passes")

func test_when_calling_is_not_Plane():
	describe("when calling asserts.is_not_Plane(null)")

	asserts.is_not_Plane(null, "Then it passes")

func test_when_calling_is_not_PoolByteArray():
	describe("when calling asserts.is_not_PoolByteArray(null)")

	asserts.is_not_PoolByteArray(null, "Then it passes")

func test_when_calling_is_not_PoolColorArray():
	describe("when calling asserts.is_not_PoolColorArray(null)")

	asserts.is_not_PoolColorArray(null, "Then it passes")

func test_when_calling_is_not_PoolIntArray():
	describe("when calling asserts.is_not_PoolIntArray(null)")

	asserts.is_not_PoolIntArray(null, "Then it passes")

func test_when_calling_is_not_PoolRealArray():
	describe("when calling asserts.is_not_PoolRealArray(null)")

	asserts.is_not_PoolRealArray(null, "Then it passes")

func test_when_calling_is_not_PoolStringArray():
	describe("when calling asserts.is_not_PoolStringArray(null)")

	asserts.is_not_PoolStringArray(null, "Then it passes")

func test_when_calling_is_not_PoolVector2Array():
	describe("when calling asserts.is_not_PoolVector2Array(null)")

	asserts.is_not_PoolVector2Array(null, "Then it passes")

func test_when_calling_is_not_PoolVector3Array():
	describe("when calling asserts.is_not_PoolVector3Array(null)")

	asserts.is_not_PoolVector3Array(null, "Then it passes")

func test_when_calling_is_not_Quat():
	describe("when calling asserts.is_not_Quat(null)")

	asserts.is_not_Quat(null, "Then it passes")

func test_when_calling_is_not_Rect2():
	describe("when calling asserts.is_not_Rect2(null)")

	asserts.is_not_Rect2(null, "Then it passes")

func test_when_calling_is_not_RID():
	describe("when calling asserts.is_not_RID(null)")

	asserts.is_not_RID(null, "Then it passes")

func test_when_calling_is_not_String():
	describe("when calling asserts.is_not_String(null)")

	asserts.is_not_String(null, "Then it passes")

func test_when_calling_is_not_Transform():
	describe("when calling asserts.is_not_Transform(null)")

	asserts.is_not_Transform(null, "Then it passes")

func test_when_calling_is_not_Transform2D():
	describe("when calling asserts.is_not_Transform2D(null)")

	asserts.is_not_Transform2D(null, "Then it passes")

func test_when_calling_is_not_Vector2():
	describe("when calling asserts.is_not_Vector2(null)")

	asserts.is_not_Vector2(null, "Then it passes")

func test_when_calling_is_not_Vector3():
	describe("when calling asserts.is_not_Vector3(null)")

	asserts.is_not_Vector3(null, "Then it passes")

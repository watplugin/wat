import { BreakableStylesDirective } from './breakable-styles.directive';

describe('DynamicStyleDirective', () => {
  it('should create an instance', () => {
    // const directive = new BreakStyleDirective();
    // expect(directive).toBeTruthy();
  });
});
